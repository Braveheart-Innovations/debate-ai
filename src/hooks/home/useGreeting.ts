/**
 * Re-export from new location for backwards compatibility.
 * The useGreeting hook has been moved to /src/hooks/useGreeting.ts
 * to support screen-specific greetings across the app.
 */
export { useGreeting } from '../useGreeting';
